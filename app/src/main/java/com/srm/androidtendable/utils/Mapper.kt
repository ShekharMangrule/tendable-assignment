package com.srm.androidtendable.utils

import com.srm.androidtendable.model.Inspection
import com.srm.androidtendable.model.UICategory

object Mapper {

    /*fun toUIModel(inspection: Inspection): UICategory {
        val categories = inspection.survey.categories
        for (item in categories) {
            return UICategory(
                id = item.id,
                name = item.name,
            )
        }
    }*/
}